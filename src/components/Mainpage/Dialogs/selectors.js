// import { createSelector } from 'reselect'

export const getDialogItem = (state) => {

   return state.messages.dialogItem
}
export const getMessag = (state) => {

   return state.messages.messag
}
